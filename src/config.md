-   `text_colors`: A list of colors and their associated shortcuts in the specified format. Any CSS color should work.
    Example:
    ```
    "text_colors": [
        {
            "color": "#ff0000",
            "shortcut": "Ctrl+Shift+R"
        }
    ]
    ```
